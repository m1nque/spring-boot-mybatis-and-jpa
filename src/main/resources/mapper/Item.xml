<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.m1nque.mybatis.demo.repository.ItemMapper">
<!--    <update id="updateUserItemQuantity" parameterType="UserItemParam">-->
<!--        UPDATE user_item-->
<!--        SET quantity = #{quantity}-->
<!--        WHERE itemId = #{itemId}-->
<!--        AND userId = #{userId}-->
<!--    </update>-->









    <update id="updateUserItemQuantity" parameterType="UserItemParam">
        UPDATE user_item
        SET quantity = #{quantity}
        WHERE item_id = #{itemId}
        AND user_id = #{userId}
    </update>
</mapper>